import sys

print(sys.version)
print(sys.version_info)
# print(sys.modules)
print(sys.path) # python imports all the modules and libraries from these paths # sys.path is a environment veriable for python
# echo "${PATH}" :- these paths are being used by system for the execution of scripts
sys.exit()
print("Hello")
